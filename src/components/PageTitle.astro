---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<style>
  h1 {
    margin: 0 0 var(--space-l) 0;
    font-size: var(--step-4);
    line-height: 1;
  }
</style>

<h1>{title}</h1>

